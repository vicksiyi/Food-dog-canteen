<!--pages/menu/index.wxml-->
<!--pages/menu/index.wxml-->
<view style="height: 50px;display: {{isNotice ? '':'none'}};">
  <van-notice-bar bind:close="closeNotice" mode="closeable" left-icon="volume-o" text="在代码阅读过程中人们说脏话的频率是衡量代码质量的唯一标准。" />
</view>
<view class="menu">
  <view class="aside">
    <van-sidebar active-key="{{ activeKey }}" bind:change="onChange">
      <block wx:for="{{asides}}" wx:key="*this">
        <van-sidebar-item title="{{item}}" />
      </block>
    </van-sidebar>
  </view>
  <view class="content" style="height: {{isNotice ? (height - 50):height}}px;">
    <!-- <van-grid column-num="3">
      <van-grid-item link-type="navigateTo" url="../detail/index" use-slot wx:for="{{ 8 }}" wx:for-item="index">
        <image  style="width: 100%; height: 90px;" src="https://img.yzcdn.cn/vant/apple-{{ index + 1 }}.jpg" />
        <span>荤素套餐{{index + 1}}</span>
      </van-grid-item>
    </van-grid> -->
    <van-card bindtap="nav" wx:for="{{40}}" price="19.8" desc="套餐描述信息{{index + 1}}" title="套餐{{index + 1}}" thumb="cloud://cloud1-6gyk321306fd5b0f.636c-cloud1-6gyk321306fd5b0f-1311839739/foods/{{index + 1}}.png" />
  </view>
</view>

<!-- 添加 -->
<view class="add">
  <van-button icon="plus" bindtap="add" round type="danger"></van-button>
</view>